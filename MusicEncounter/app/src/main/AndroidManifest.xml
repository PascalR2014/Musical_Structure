<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.musicencounter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BienvenueActivity"
            android:label="@string/category_bienvenue"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SearchActivity"
            android:label="@string/category_search"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".InfoActivity"
            android:label="@string/category_info"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PlayerActivity"
            android:label="@string/category_player"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".PlaylistActivity"
            android:label="@string/category_playlist"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".ImplicitActivity"
            android:label="@string/category_implicit"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>